# ForCli — Python Backend Requirements
# ──────────────────────────────────────
# Install with: pip install -r requirements.txt

# ── Core Django ───────────────────────
Django==5.2.1
djangorestframework==3.16.1
djangorestframework_simplejwt==5.5.1
django-cors-headers==4.9.0
django-filter==25.2

# ── Utilities ─────────────────────────
dj-database-url==2.3.0
python-dotenv==1.2.2
whitenoise==6.9.0
pillow==11.2.1
sqlparse==0.5.3
asgiref==3.8.1

# ── Optional: MySQL on cPanel ──────────
# Uncomment if switching from SQLite to MySQL:
# mysqlclient==2.2.4

# NOTE: gunicorn is NOT needed for cPanel (uses Passenger WSGI)
